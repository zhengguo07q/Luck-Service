spring:
  application:
    name: luck-service-auth
  cloud:
    config:
      uri: http://luck-config:8888
      fail-fast: true
      #password: ${CONFIG_SERVICE_PASSWORD}
      password: root
      username: user
